import { MinuteValidatorDirective } from './minute-validator.directive';

describe('MinuteValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinuteValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
