import { HkeValidatorDirective } from './hke-validator.directive';

describe('HkeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HkeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
