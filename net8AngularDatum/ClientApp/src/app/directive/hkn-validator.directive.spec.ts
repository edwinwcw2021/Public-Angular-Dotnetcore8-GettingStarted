import { HknValidatorDirective } from './hkn-validator.directive';

describe('HknValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HknValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
