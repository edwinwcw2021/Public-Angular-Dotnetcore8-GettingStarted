import { DegreeValidatorDirective } from './degree-validator.directive';

describe('DegreeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DegreeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
